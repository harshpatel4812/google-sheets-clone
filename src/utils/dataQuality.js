export const TRIM = (value) => value.trim();
export const UPPER = (value) => value.toUpperCase();
export const LOWER = (value) => value.toLowerCase();
export const REMOVE_DUPLICATES = (rows) => [...new Set(rows)];
